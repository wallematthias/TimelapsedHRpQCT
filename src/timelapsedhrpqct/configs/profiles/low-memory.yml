profile: low-memory
description: Lower peak memory settings for large multistack studies.

multistack_correction:
  enabled: true
  debug: false

fusion:
  enable_filling: false

analysis:
  space: baseline_common
  use_filled_images: false

visualization:
  enabled: false
