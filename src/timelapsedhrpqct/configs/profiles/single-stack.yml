profile: single-stack
description: Profile for datasets that should skip multistack correction.

multistack_correction:
  enabled: false

analysis:
  space: pairwise_fixed_t0
  pair_mode: adjacent

visualization:
  enabled: true
